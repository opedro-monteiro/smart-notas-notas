// import type { FastifyInstance } from "fastify";

// Placeholder for JWT plugin registration if Clerk is replaced or supplemented.
//_app: FastifyInstance
export async function jwtPlugin() {
  // Register JWT plugin here when needed.
}
